# OAuth 2.0 credentials from developer.atlassian.com
# These are injected at build time for private distribution
# Without these, users will be prompted to enter their own OAuth credentials

DOCFETCH_CLIENT_ID=your-client-id-here
DOCFETCH_CLIENT_SECRET=your-client-secret-here
